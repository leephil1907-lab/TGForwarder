Production session persistence requirements: Telegram StringSession must persist in TG_DATA_DIR tenant storage across browser visits and Railway restarts. Closing/refreshing the website must not disconnect Telegram. Only explicit Disconnect clears the Telegram authorization/session. Application session cookies remain device/tenant scoped and must not expose another tenant's Telegram state.